# Default parameter grid for deletion capacity experiments
# Based on the grid specified in AGENTS.md

# Unified gamma parameter
gamma_bar: [1.0]
gamma_split: [0.9, 0.7, 0.5, 0.3]

# Calibrator conservativeness  
quantile: [0.90]

# Delete workload
delete_ratio: [1, 5]

# Accountant type
accountant: ["legacy"]

# Privacy budget (optional)
eps_total: [1.0]